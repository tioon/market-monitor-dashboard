{
  "name": "market-monitor-dashboard",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "sync:data": "node scripts/sync-data.mjs",
    "build": "npm run sync:data && vite build",
    "preview": "vite preview",
    "test": "node --test tests/metrics.test.mjs"
  },
  "dependencies": {
    "react": "^19.1.0",
    "react-dom": "^19.1.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.6.0",
    "vite": "^7.1.0"
  }
}
